{
  "name": "@morganestes/system-report",
  "version": "0.1.0",
  "main": "Gruntfile.js",
  "author": "Morgan Estes",
  "repository": "git@github.com:morganestes/system-report.git",
  "private": true,
  "devDependencies": {
    "grunt": "^1.0.2",
    "grunt-phpcs": "^0.4.0",
    "grunt-wp-i18n": "^1.0.2",
    "grunt-wp-readme-to-markdown": "^2.0.1"
  },
  "scripts": {
    "autoload": "composer install --no-dev -o -q --no-progress --no-scripts",
    "cleanup": "rm -rf vendor && npm run autoload"
  }
}
